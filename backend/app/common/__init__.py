"""
StudyFlow AI — Common Utilities
─────────────────────────────────────────────────────────────
Shared building blocks used across all feature modules:

  exceptions.py   — domain exceptions (transport-agnostic)
  responses.py    — standard API response envelope
  dependencies.py — shared FastAPI dependency re-exports
"""
